#!/usr/bin/env bash
# bin/release <build-dir>
set -e

BUILD_DIR=$(cd "$1/" && pwd)

echo "ERROR, cf-cloud-sql-proxy-buildpack is not a final builpack!"
ehco "Please check the order of the buildpacks you are using"

exit 1

